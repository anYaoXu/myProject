import { Scrollbar.Directive.TsDirective } from './scrollbar.directive.ts.directive';

describe('Scrollbar.Directive.TsDirective', () => {
  it('should create an instance', () => {
    const directive = new Scrollbar.Directive.TsDirective();
    expect(directive).toBeTruthy();
  });
});
