import { PopExportDirective } from './pop-export.directive';

describe('PopExportDirective', () => {
  it('should create an instance', () => {
    const directive = new PopExportDirective();
    expect(directive).toBeTruthy();
  });
});
