import { AppPopImportDirective } from './pop-import.directive';

describe('AppPopImportDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPopImportDirective();
    expect(directive).toBeTruthy();
  });
});
